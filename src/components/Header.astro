---
// 檔案路徑: src/components/Header.astro
// (這個元件很單純，不需要 --- 區塊的邏輯)
---
<header class="bg-[var(--bg-card)] shadow-sm border-b border-[var(--border-color)] sticky top-0 z-50">
    <div class="container mx-auto px-6 py-4 flex flex-wrap md:flex-nowrap justify-between items-center">
        <a href="/" class="flex items-center space-x-3">
            <img 
                src="/images/kae_logo.png" 
                alt="KAE 的大頭照" 
                class="w-10 h-10 mx-auto rounded-full"
            >
            <h1 class="text-lg md:text-2xl font-bold text-[var(--text-primary)] font-['Zen_Maru_Gothic']">KAE的雜記</h1>
        </a>
        
        <button id="mobile-menu-button" class="md:hidden p-2 rounded-md text-[var(--text-primary)] hover:bg-gray-100">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
        </button>
        
        <div id="mobile-menu-content" class="hidden md:flex flex-col md:flex-row md:items-center space-y-4 md:space-y-0 md:space-x-6 w-full md:w-auto mt-4 md:mt-0">
            
            <nav class="w-full md:w-auto">
                <!-- 
                  選單文字靠左 (手機和電腦版都是靠左)
                -->
                <ul class="flex flex-col md:flex-row space-y-2 md:space-y-0 md:space-x-6 text-left">
                    <li><a href="/" class="nav-link text-lg text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition-colors">首頁</a></li>
                    <li><a href="/archive" class="nav-link text-lg text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition-colors">所有文章</a></li>
                    <li><a href="/about" class="nav-link text-lg text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition-colors">關於我</a></li>
                </ul>
            </nav>
            
            <!-- 
              👇 修改重點：搜尋框手機版置中，電腦版靠左
              原：justify-start
              改：justify-center md:justify-start
            -->
            <form action="/archive" method="GET" class="flex justify-center md:justify-start items-center space-x-2 w-full md:w-auto">
                <input 
                  type="text" 
                  name="q" 
                  placeholder="搜尋文章..." 
                  class="px-3 py-2 rounded-md border border-[var(--border-color)] text-sm text-[var(--text-primary)] bg-white focus:outline-none focus:ring-2 focus:ring-[var(--accent-color)]"
                >
                <button 
                  type="submit" 
                  class="px-4 py-2 bg-[var(--accent-color)] text-white text-sm rounded-md hover:bg-[var(--accent-hover)] transition"
                >
                  搜尋
                </button>
            </form>
        </div>
    </div>
</header>
