---
// src/layouts/BaseLayout.astro (已加入 OG 系統)
import { ViewTransitions } from 'astro:transitions';
import "../styles/global.css";

// 1. 載入我們的新元件
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

export interface Props {
    pageTitle?: string;
    description?: string;
    ogImage?: string; // 👈 (新) 加入 ogImage 屬性
}

// (新) 解構 ogImage
const { pageTitle, description, ogImage } = Astro.props; 

const defaultTitle = "KAE的雜記 - 日本留學與生活";
const defaultDesc = "KAE です！ 這個部落格會來記錄我即將展開的日本交換生活，也會分享留學的事前申請、日本旅遊等經驗。";

const seoTitle = pageTitle ? `${pageTitle} | KAE的雜記` : defaultTitle;
const seoDescription = description || defaultDesc;

// 預設 OG 圖路徑，您需要在 public/images/ 上傳此圖
const DEFAULT_OG_IMAGE = "/images/og_default.png";
---
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{seoTitle}</title>
    <meta name="description" content={seoDescription} />
    
    <link rel="icon" type="image/png" href="/images/kae_favicon.png">
    <link rel="apple-touch-icon" href="/images/kae_favicon.png">

    <meta name="google-site-verification" content="RSCsK8X-OoCTr2uKZ2oKz-QbY1SjZWYLZRIfQz9NKWY" />
    
    <meta property="og:title" content={seoTitle} />
    <meta property="og:description" content={seoDescription} />
    <meta property="og:type" content="website" />
    
    <meta property="og:image" content={ogImage || DEFAULT_OG_IMAGE} /> 
    
    <meta name="twitter:card" content="summary_large_image" /> 

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Noto+Sans+TC:wght@400;500;700&display=swap" rel="stylesheet">
    <ViewTransitions />

     <!-- 
      ▼▼▼ 修正：加回 Noto Sans JP (日文) ▼▼▼ 
    -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Noto+Sans+JP:wght@400;500;700&family=Noto+Sans+TC:wght@400;500;700&display=swap" rel="stylesheet">
    
    <ViewTransitions />   

</head>

<body class="bg-[var(--bg-main)]">

    <Header />

    <main class="container mx-auto px-6 py-12">
        <slot />
    </main>

    <Footer />

    <script>
        function setupMenu() {
            const menuButton = document.getElementById('mobile-menu-button');
            const menuContent = document.getElementById('mobile-menu-content');
            if (menuButton && menuContent) {
                menuButton.onclick = () => {
                    menuContent.classList.toggle('hidden');
                };
            }
        }
        setupMenu();
        document.addEventListener('astro:page-load', setupMenu);
    </script>
</body>
</html>
